version: '3.8'
services:
  backend:
    build: ./backend
    volumes: ./backend:/usr/src/app
    ports: "8080:8080"
  frontend:
    build: ./frontend
    volumes: ./frontend:/home/node/app
    ports: "8081:80"
